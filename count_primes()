```python
    def count_primes2(num):
    is_prime_count = 0
    primes = []
    for i in range(2, num+1):
        for j in range(2, i):
            if (i % j) == 0:
                break
        else:
            primes.append(i)
            is_prime_count += 1
    print(primes)
    
    return is_prime_count

```
